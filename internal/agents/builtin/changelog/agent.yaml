name: changelog
description: "Generate human-readable reports on interesting activity in a git repo"
time_grounding: true
agents_md: "true"
skills: "all"

tools:
  enabled: [read_file, grep, shell]

shell:
  allow: ["git *"]
  auto_run: true

max_turns: 200
