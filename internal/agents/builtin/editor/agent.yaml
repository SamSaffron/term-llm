name: editor
description: "File editing assistant"
time_grounding: true
agents_md: "true"
skills: "all"

tools:
  enabled: [read_file, write_file, edit_file, glob, grep]

search: true

max_turns: 200
