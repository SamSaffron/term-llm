name: agent-builder
description: "Create and edit custom agents interactively"
time_grounding: true
skills: "all"

tools:
  enabled: [read_file, write_file, edit_file, glob, grep, ask_user]

search: true

max_turns: 200
