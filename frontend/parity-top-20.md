# Legacy web UI parity: ranked top 20

This backlog records the Fable-assisted audit performed on 2026-08-25. The reference implementation is `/tmp/term-llm/internal/serveui/static`; the replacement is `frontend/src`. Items are ranked by broken user workflow and data loss first, then interaction frequency and visual impact.

Status: **open**, **in progress**, or **done**. “Done” requires a regression test using the real server response shape or an observable UI test, not only matching markup.

| Rank | Status | Divergence | Legacy behavior and acceptance criteria | Primary owners |
|---:|---|---|---|---|
| 1 | done | Runtime selector is empty and visually regressed | Parse the server's `{object:"list",data:[...]}` provider/model payloads; retain provider metadata and model reasoning metadata. Restore the compact runtime chip, default-runtime labels, effort meter, and one provider/model/effort popover. Provider changes clear stale models immediately; stale requests cannot win; provider/model are locked during a response while effort remains queueable. Reference: `app-runtime.js`, `chip_picker_test.js`. | `stores/app-store.ts`, `domain/runtime.ts`, `components/Header.tsx` |
| 2 | done | Diff list/detail use the wrong wire shape | Consume `file_changes` and hunk-based detail responses, including binary/image files, additions/deletions, scope, and live refresh. A real Go-handler response must render files and hunks. Reference: `app-diffs.js`, `app_diff*_test.js`. | `api/endpoints.ts`, `domain/diff.ts`, `stores/app-store.ts`, `components/Panels.tsx` |
| 3 | in progress | Inline diff comments and queue transport are missing/broken | Restore line selection, range comments, edit/delete, persistent per-session queue, send-now/next-message behavior, limits, discard, and history. Submit `diff_comment` input parts through response/interrupt contracts rather than the nonexistent simplified endpoint. Reference: `app-diff-comments.js`, `app-diff-queue.js`. | `stores/app-store.ts`, `components/Panels.tsx`, `platform/storage.ts` |
| 4 | done | No-project mode can fail bootstrap/sidebar | A disabled-project server must still open chat, list ungrouped sessions, create a new chat, and expose appropriate worktree behavior. Sidebar parsing must accept the actual no-project response without requiring project groups. Reference: `app-sidebar.js`, `app_sidebar_test.js`. | `stores/app-store.ts`, `components/Sidebar.tsx` |
| 5 | done | Tool calls lost legacy grouping and compact formatting | Consecutive calls are grouped by assistant turn; common tools get concise summaries/icons; completed groups collapse; running/failed state remains visible; expanded state survives streaming replacement. Agent/subagent tool storms must not become a wall of cards. Reference: `app-render.js`, `app_render_test.js`. | `domain/transcript.ts`, `components/Transcript.tsx` |
| 6 | done | Project creation is a raw path form | Restore folder browsing with parent/child navigation, hidden-file toggle, the real `?path=&show_hidden=` API, selected-folder preview/dry run, project name, duplicate-project handling, unavailable paths, and create/assign actions. Reference: `app-project-picker.js`. | `api/endpoints.ts`, `components/Modals.tsx`, `stores/app-store.ts` |
| 7 | done | Conversation tree appears too early and lacks branch semantics | Show Paths only when `path_count` indicates a branch point. Restore branch chooser/tree points, active path, origin labels, child links, path notes, branch-context preparation, and focus/scroll behavior. Reference: `app-branching.js`, `app-branch-commands.js`. | `components/Header.tsx`, `components/Modals.tsx`, `stores/app-store.ts` |
| 8 | done | Transcript adds role labels the legacy UI did not have | Remove “Assistant”/“User” chrome, restore compact relative time and model/effort metadata, queued-effort markers, turn usage/token formatting, cancellation/interruption badges, and model-swap/compaction rows. Reference: `app-render.js`. | `components/Transcript.tsx`, `domain/transcript.ts` |
| 9 | in progress | Diff context expansion and navigation are absent | Restore hidden-context separators (“Show N hidden lines”), anchor-preserving expansion, expand-all/bulk controls, next/previous file navigation, filtering, maximize, and follow-current-file behavior. Reference: `app-diff-context.js`, `app-diffs.js`. | `domain/diff.ts`, `components/Panels.tsx` |
| 10 | open | Worktree picker/actions are simplified | Restore compact chip/popover, root/current selection, create flow, dirty/ahead/behind state, diff preview, merge/promote/remove confirmation, force recovery, and session-lock rules. Reference: `app-worktrees.js`, `app_worktrees_test.js`. | `components/Header.tsx`, `components/Modals.tsx`, `stores/app-store.ts` |
| 11 | open | Sidebar project/session administration is incomplete | Restore project menus (rename/archive/restore), unavailable-project state, per-project loading/counts, no-project group, session pin/hide/restore/title actions, category search, and stable menu keyboard behavior. Reference: `app-sidebar.js`, `app-session-admin.js`. | `components/Sidebar.tsx`, `stores/app-store.ts` |
| 12 | open | Plan panel lacks legacy attention and responsive behavior | Restore unseen-change dot, current-step emphasis, progress, open/close coordination, mobile sheet/backdrop, swipe-to-dismiss, and appropriate auto-open behavior. Reference: `app-plan.js`, `app_plan_test.js`. | `components/Panels.tsx`, `components/Header.tsx` |
| 13 | open | Attachments/drop experience is incomplete | Restore drag/drop overlay, paste/drop acceptance, file/media cards, upload/removal progress, reserved image geometry, unsupported/oversize errors, and transcript attachment details. Reference: `app-attachments.js`. | `components/Composer.tsx`, `components/Transcript.tsx`, `stores/app-store.ts` |
| 14 | open | Code and math blocks lost local actions/parity | Restore copy controls and copied feedback for code/math, syntax aliases/language coverage, math fallback/error handling, and strictly local overflow without widening chat. Reference: `decoration.js`, `markdown_test.js`. | `components/Markdown.tsx`, `domain/markdown.ts`, `domain/rich-highlight.ts` |
| 15 | open | Notifications are configuration-only | Restore permission/request flow, push subscription state, foreground/background completion notifications, notification preference feedback, and session navigation from notifications. Reference: `app-core.js`, `sw.js`. | `platform/browser.ts`, `stores/app-store.ts`, settings UI |
| 16 | open | Voice/transcription feedback is reduced | Restore capability gating, recording timer/state, cancel/error behavior, audio lifecycle, transcription progress, and safe prompt insertion. Reference: `app-composer.js`. | `platform/voice.ts`, `components/Composer.tsx` |
| 17 | open | Network/version status is abrupt or invisible | Restore compact offline/reconnecting states, reconnect feedback, guarded/coalesced version mismatch refresh, and visible refresh status rather than silently hard-reloading. Reference: `app-network.js`, `app_core_test.js`. | `api/client.ts`, `platform/browser.ts`, `components/Header.tsx` |
| 18 | open | Media lightbox controls are incomplete | Restore image/video lightbox navigation, close/keyboard behavior, zoom/reset/download controls, caption/error state, and mobile-safe sizing. Reference: `app-core.js`. | `app/App.tsx`, lightbox component/styles |
| 19 | open | MCP/skills/secondary runtime surfaces are underformatted | Restore MCP availability/enabled/error states, searchable toggles, skill source/execution labels, isolated-run status/cancel links, and accurate session ownership. Reference: `app-mcp.js`, `app-skills.js`. | `components/Modals.tsx`, `stores/app-store.ts`, `components/Transcript.tsx` |
| 20 | open | Mobile panels and modal interaction lost polish/accessibility | Restore bottom-sheet positioning in the visual viewport, safe areas, swipe drag state, backdrops, focus return/trapping, Escape semantics, reduced-motion handling, and touch-sized actions across runtime/diff/plan/project/worktree surfaces. Reference: `app-modals.js`, `app-runtime.js`, panel CSS/tests. | shared components and `styles/app.css` |

## Already corrected during the audit

These regressions were fixed before this ranked implementation pass: Hub media URL rebasing, intentional model-request abort handling, skill session ownership headers, syntax-highlighting failure isolation, sidebar Hub alignment, legacy turn-copy placement/state, whole-chat horizontal overflow, and animated transcript startup scrolling.

## Implementation order

1. Repair wire contracts before visual work can mask empty data.
2. Port the legacy interaction and its focused tests in the same batch.
3. Correct browser/e2e fixtures to use the Go handler's exact response shape.
4. Update this table only after focused tests, full frontend tests, and `make build` pass.
